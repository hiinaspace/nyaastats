{
  "name": "nyaastats-website",
  "version": "0.1.0",
  "description": "Nyaa torrent statistics visualization website",
  "type": "module",
  "scripts": {
    "treemaps": "node scripts/generate-treemaps.js",
    "prebuild": "npm run treemaps",
    "predev": "npm run treemaps",
    "build": "observable build",
    "dev": "observable preview",
    "deploy": "npm run build && observable deploy"
  },
  "devDependencies": {
    "@observablehq/framework": "^1.0.0"
  },
  "dependencies": {
    "d3-hierarchy": "^3.1.2",
    "feed": "^5.2.0",
    "sharp": "^0.34.2"
  }
}
